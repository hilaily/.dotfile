### 使用

```shell
git clone https://github.com/hilaily/.dotfile.git ~/.dotfile
```

首先安装 fish

`brew install fish` 或者 `apt install fish`

然后初始化 fish

```bash
bash ~/.dotfile/script/fish-install.sh
```
